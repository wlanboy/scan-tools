[project]
name = "scan"
version = "0.1.0"
description = "Scan scripts"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pyright>=1.1.411",
    "pytest>=8.4.2",
    "ruff>=0.16.1",
]
